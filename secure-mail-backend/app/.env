DATABASE_URL=postgresql://username:password@localhost:5432/securemail
"""Replace:

    username with your PostgreSQL username.
    password with your database password.
    localhost and port if different from default (5432).
    securemail is your database name.